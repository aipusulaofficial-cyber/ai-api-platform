apiVersion: apps/v1
kind: Deployment
metadata: {name: ai-api-platform}
spec:
 replicas: 2
 selector: {matchLabels: {app: ai-api-platform}}
 template:
  metadata: {labels: {app: ai-api-platform}}
  spec:
   containers:
   - name: api
     image: ai-api-platform:latest
     ports: [{containerPort: 8000}]
     readinessProbe: {httpGet: {path: /health/ready, port: 8000}}
     livenessProbe: {httpGet: {path: /health/live, port: 8000}}
